# Dev Log — 2026-04-27-2317 (GMT/UTC)

**Project:** Coastal Telemetry Interface  
**Previous log:** `2026-04-27-2302.md`  
**Session scope:** Codify canonical dev-log location across repos, designate this codebase as backend (`coastal-telemetry-backend`), update Git remote URL, add root README; attempt local folder rename.

---

## Since Last Log

The prior entry summarized the frontend/backend boundary and documented the ticking ingest + DTO serving story. This session enforced **repository boundaries for Railway-style deploy** while keeping **one gapless dev log**: update `dev-log/README.md` so frontend-only sessions still produce entries **beside this README**, align `project-notes.md` with **backend repo identity** and GitHub naming, add a **root README**, and rename the Git remote toward **`coastal-telemetry-backend`**.

---

## Work Completed

### Dev log README — canonical folder rule
- Added **Canonical location** guidance: backend repo (`coastal-telemetry-backend`) owns **`dev-log/` next to `README.md`**; frontend repo must **not** duplicate logs; frontend-focused sessions **commit logs here**.
- Added **Rule 9** and **Step 4** clarification (switch editors to this repo before writing files).
- Extended **Quality bar** with checklist that new files live under backend `dev-log/`, not the frontend repo.
- **Outcome:** Explicit procedure for AI and human workflows when work happens outside this tree.

### Project notes — repo layout and naming
- Documented **Repository layout (deploy split)** and linked **backend repo** + **`coastal-telemetry-backend`** GitHub rename from `coastal-telemetry-interface`.
- Added bullets under **Backend Architecture Decision**: this repo is backend home and owns **`dev-log/`** for all streams.
- **Outcome:** Single source for “where does history live?” alongside architecture.

### Root README
- Added `README.md` describing backend role, frontend elsewhere, pointer to **`dev-log/`**, and instructions for matching **GitHub repository name** and **`git remote`**.
- **Outcome:** Repository landing page explains deploy split without duplicating internal design notes.

### Git remote toward renamed GitHub repo
- Set `origin` to `https://github.com/DawsonJay/coastal-telemetry-backend.git`.
- **Why:** Align local clone with planned repository name **`coastal-telemetry-backend`** for portfolio and Railway alignment.
- **Outcome:** Ready to push once GitHub repo is renamed or created under that slug.

### Local folder rename (partial)
- Attempted `Rename-Item` … `coastal-telemetry-interface` → `coastal-telemetry-backend`; **blocked** (`InvalidOperation`: path in use — IDE/workspace lock).
- Documented manual rename steps in root **README.md** after closing Cursor.
- **Outcome:** Naming intent recorded; user completes rename when folder is unlocked.

---

## Technical Decisions and Trade-offs

- **Decision:** Single canonical **`dev-log/`** in backend repo regardless of session topic.  
  **Alternatives considered:** Duplicate logs in frontend repo or separate docs-only repo.  
  **Why chosen:** One chronological portfolio story; avoids split history.

- **Decision:** GitHub repo name **`coastal-telemetry-backend`** (replacing `-interface`).  
  **Alternatives considered:** Keep old name only.  
  **Why chosen:** Signals backend role explicitly when paired with a future frontend repo.

---

## Commands and Tests Run

- Timestamp (GMT/UTC):  
  `powershell -Command "(Get-Date).ToUniversalTime().ToString('yyyy-MM-dd-HHmm')"`  
  Result: `2026-04-27-2317`

- Remote update:  
  `git remote set-url origin https://github.com/DawsonJay/coastal-telemetry-backend.git`

- Folder rename — failed while folder in use; documented workaround in `README.md`.

---

## Problems Encountered

- **Problem:** Cannot rename workspace directory while Cursor/IDE holds locks.  
  **Diagnosis:** Windows `Rename-Item` reported path in use.  
  **Resolution:** Documented closing IDE and renaming via PowerShell snippet in root README.

- **Problem:** Until GitHub repository is renamed to `coastal-telemetry-backend`, **`git push` may fail** if that slug does not exist yet.  
  **Diagnosis:** Remote URL anticipates renamed repo; GitHub state is user's account.  
  **Resolution:** Rename repo in GitHub Settings (or create new repo) — GitHub redirects old URLs for renames when applicable.

---

## Portfolio Value Notes

- Demonstrates **mono-history discipline** across split deploy repos—common senior concern for micro-frontend/backend teams.
- Shows **operational maturity**: repo naming aligned with architectural role (API vs UI).
- Quantitative N/A this session except **remote URL** verification (explicit before/after not needed for prose).

---

## Highlight Flags

- **Skill signal:** Repository strategy for split Railway services without fragmenting audit trail.
- **Approach signal:** Documentation-first contract for cross-repo discipline (where logs live).
- **Character signal:** Honest recording of tooling limits (rename blocked) with clear user-facing remediation.
- **Employer relevance:** Mirrors real teams managing multiple deployables under one program narrative.

---

## Next Actions

- **User:** Rename GitHub repository **DawsonJay/coastal-telemetry-interface** → **`coastal-telemetry-backend`** (Settings → Repository name), then `git push`.
- **User:** Close IDE; rename local folder **`coastal-telemetry-interface`** → **`coastal-telemetry-backend`**; reopen workspace from new path.
- Scaffold **`coastal-telemetry-web`** (or chosen name) when frontend work begins; README there should link back to **`dev-log/`** instructions in **this** repo only.
